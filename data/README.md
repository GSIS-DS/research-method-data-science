# Course data

`sample/` contains small release-safe teaching data. `external/` contains only instructions for data
that must be obtained elsewhere. Each dataset must document origin, license/usage status, variables,
and known limitations. Never add confidential student or human-subject data.
